import 'package:flutter/cupertino.dart';

import '../constans.dart';

class WedgiteComponts {
  static var containerBorderandColor = BoxDecoration(

       border: Border.all(),
                                borderRadius: BorderRadius.circular(4.0),
                                color: KColorecart,
  );

  static var containerBorderandunColor = BoxDecoration(

       border: Border.all(color: KColorecart),
                                borderRadius: BorderRadius.circular(4.0),
                                
                                 );
}
